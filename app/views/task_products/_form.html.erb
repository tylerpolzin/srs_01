<%= form_for(task_product) do |f| %>
  <% if task_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task_product.errors.count, "error") %> prohibited this task_product from being saved:</h2>

      <ul>
      <% task_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
