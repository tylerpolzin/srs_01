<p id="notice"><%= notice %></p>

<h1>Warrantyorders</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @warrantyorders.each do |warrantyorder| %>
      <tr>
        <td><%= link_to 'Show', warrantyorder %></td>
        <td><%= link_to 'Edit', edit_warrantyorder_path(warrantyorder) %></td>
        <td><%= link_to 'Destroy', warrantyorder, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Warrantyorder', new_warrantyorder_path %>
